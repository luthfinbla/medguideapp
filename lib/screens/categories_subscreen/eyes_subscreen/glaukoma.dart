import 'package:flutter/material.dart';

class Glaukoma extends StatelessWidget {
  const Glaukoma({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('glauko'),
      ),
    );
  }
}
