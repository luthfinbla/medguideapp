import 'package:flutter/material.dart';

class Amblyopia extends StatelessWidget {
  const Amblyopia({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('amblyoi'),
      ),
    );
  }
}
