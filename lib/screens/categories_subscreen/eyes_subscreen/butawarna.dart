import 'package:flutter/material.dart';

class Butawarna extends StatelessWidget {
  const Butawarna({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('butawa'),
      ),
    );
  }
}
