import 'package:flutter/material.dart';

class Naturs extends StatelessWidget {
  const Naturs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Naturs'),
      ),
    );
  }
}
