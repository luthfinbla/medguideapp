import 'package:flutter/material.dart';

class Matovit extends StatelessWidget {
  const Matovit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Matovit'),
      ),
    );
  }
}
