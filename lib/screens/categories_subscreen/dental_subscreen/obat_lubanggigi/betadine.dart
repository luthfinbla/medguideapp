import 'package:flutter/material.dart';

class Betadine extends StatelessWidget {
  const Betadine({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Betadine'),
      ),
    );
  }
}
