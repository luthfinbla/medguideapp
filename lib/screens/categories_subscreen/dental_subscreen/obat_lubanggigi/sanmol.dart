import 'package:flutter/material.dart';

class Sanmol extends StatelessWidget {
  const Sanmol({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Sanmol'),
      ),
    );
  }
}
