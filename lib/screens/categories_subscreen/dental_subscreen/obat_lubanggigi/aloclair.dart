import 'package:flutter/material.dart';

class Aloclair extends StatelessWidget {
  const Aloclair({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Aloclair'),
      ),
    );
  }
}
