import 'package:flutter/material.dart';

class Periodontitis extends StatelessWidget {
  const Periodontitis({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Periodontitis'),
      ),
    );
  }
}
